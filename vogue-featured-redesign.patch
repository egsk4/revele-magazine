diff --git a/artifacts/revele-web/index.html b/artifacts/revele-web/index.html
index efc5888..37e9a4b 100644
--- a/artifacts/revele-web/index.html
+++ b/artifacts/revele-web/index.html
@@ -661,27 +661,75 @@ nav {
 .featured-wrap { padding: 0 20px 48px; max-width: 1160px; margin: 0 auto; display: flex; flex-direction: column; gap: 18px; }
 .featured-header { background: var(--bg-1); border: 1px solid var(--border); border-radius: var(--radius); padding: 48px; }
 .featured-empty { text-align: center; padding: 80px 20px; font-size: 0.74rem; color: var(--text-3); letter-spacing: 0.1em; }
-.featured-grid { display: grid; grid-template-columns: repeat(3,1fr); gap: 12px; }
-.featured-card {
-  background: var(--bg-1); border: 1px solid var(--border); border-radius: var(--radius);
-  padding: 30px; display: flex; flex-direction: column; gap: 10px;
-  cursor: pointer; transition: all 0.25s; position: relative; overflow: hidden;
+/* --- cover story (hero) --- */
+.featured-hero {
+  position: relative; width: 100%; height: 62vh; min-height: 420px; max-height: 640px;
+  border-radius: var(--radius); overflow: hidden; cursor: pointer;
+  background: var(--bg-1); border: 1px solid var(--border);
 }
-.featured-card::before {
-  content: ''; position: absolute; top: -40px; right: -40px;
-  width: 130px; height: 130px;
-  background: radial-gradient(circle, rgba(200,160,106,0.06) 0%, transparent 70%);
-  pointer-events: none;
+.featured-hero img {
+  position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover;
+  transition: transform 1.1s cubic-bezier(0.16,1,0.3,1); transform: scale(1.0);
+}
+.featured-hero:hover img { transform: scale(1.045); }
+.featured-hero::after {
+  content: ''; position: absolute; inset: 0;
+  background: linear-gradient(to top, rgba(0,0,0,0.92) 0%, rgba(0,0,0,0.5) 42%, rgba(0,0,0,0.05) 72%, transparent 100%);
+}
+.featured-hero-copy { position: absolute; left: 0; right: 0; bottom: 0; padding: 44px 48px; z-index: 1; }
+.featured-hero-kicker { font-size: 0.62rem; font-weight: 500; letter-spacing: 0.14em; text-transform: uppercase; color: var(--gold-2); display: flex; align-items: center; gap: 10px; margin-bottom: 14px; }
+.featured-hero-kicker::before { content: ''; width: 20px; height: 1px; background: var(--gold-2); }
+.featured-hero-title { font-family: 'Cormorant Garamond', serif; font-size: clamp(2.4rem, 5.5vw, 4.4rem); font-weight: 300; font-style: italic; line-height: 1.02; letter-spacing: 0.01em; color: #fdfcf9; max-width: 780px; }
+.featured-hero-by { margin-top: 16px; font-size: 0.76rem; font-weight: 300; color: rgba(255,255,255,0.72); }
+.featured-hero-by strong { color: #fff; font-weight: 400; }
+.featured-hero-by span { color: var(--gold-2); }
+
+/* --- editorial mosaic grid --- */
+.featured-grid { display: grid; grid-template-columns: repeat(3,1fr); gap: 12px; margin-top: 12px; grid-auto-flow: dense; }
+.featured-card {
+  position: relative; border-radius: var(--radius); overflow: hidden; cursor: pointer;
+  border: 1px solid var(--border); background: var(--bg-1);
+  aspect-ratio: 3/4; transition: border-color 0.3s;
+}
+.featured-card.is-wide { grid-column: span 2; aspect-ratio: 16/10; }
+.featured-card:hover { border-color: rgba(200,160,106,0.4); }
+.featured-card img {
+  position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover;
+  transition: transform 0.9s cubic-bezier(0.16,1,0.3,1);
+}
+.featured-card:hover img { transform: scale(1.06); }
+.featured-card::after {
+  content: ''; position: absolute; inset: 0;
+  background: linear-gradient(to top, rgba(0,0,0,0.88) 0%, rgba(0,0,0,0.35) 45%, transparent 75%);
+  transition: background 0.3s;
 }
-.featured-card:hover { border-color: rgba(200,160,106,0.35); transform: translateY(-2px); }
-.featured-card-issue { font-size: 0.58rem; font-weight: 500; letter-spacing: 0.12em; text-transform: uppercase; color: var(--gold); }
-.featured-card-series { font-family: 'Cormorant Garamond', serif; font-size: 1.25rem; font-weight: 300; font-style: italic; line-height: 1.3; margin-top: 2px; }
-.featured-card-by { font-size: 0.66rem; font-weight: 300; color: var(--text-2); }
-.featured-card-by strong { color: var(--text); font-weight: 400; }
-.featured-card-insta { font-size: 0.64rem; font-weight: 400; color: var(--gold); }
-.featured-card-excerpt { font-size: 0.72rem; font-weight: 300; line-height: 1.8; color: var(--text-2); display: -webkit-box; -webkit-line-clamp: 4; -webkit-box-orient: vertical; overflow: hidden; flex: 1; }
-.featured-card-read { font-size: 0.58rem; font-weight: 500; letter-spacing: 0.1em; text-transform: uppercase; color: var(--gold); margin-top: auto; display: flex; align-items: center; gap: 8px; }
+.featured-card-copy { position: absolute; left: 0; right: 0; bottom: 0; padding: 22px 24px; z-index: 1; }
+.featured-card-issue { font-size: 0.56rem; font-weight: 500; letter-spacing: 0.12em; text-transform: uppercase; color: var(--gold-2); }
+.featured-card-series { font-family: 'Cormorant Garamond', serif; font-size: 1.4rem; font-weight: 300; font-style: italic; line-height: 1.2; margin-top: 5px; color: #fdfcf9; }
+.featured-card-by { font-size: 0.66rem; font-weight: 300; color: rgba(255,255,255,0.68); margin-top: 6px; }
+.featured-card-by strong { color: #fff; font-weight: 400; }
+.featured-card-read {
+  font-size: 0.56rem; font-weight: 500; letter-spacing: 0.1em; text-transform: uppercase; color: var(--gold-2);
+  margin-top: 10px; display: flex; align-items: center; gap: 8px;
+  opacity: 0; transform: translateY(4px); transition: opacity 0.3s, transform 0.3s;
+}
+.featured-card:hover .featured-card-read { opacity: 1; transform: translateY(0); }
 .featured-card-read::after { content: '→'; font-size: 0.8rem; letter-spacing: 0; }
+
+/* --- fallback text card, used only when an entry has no images yet --- */
+.featured-card--text {
+  aspect-ratio: auto; padding: 30px; display: flex; flex-direction: column; gap: 10px;
+  transition: all 0.25s;
+}
+.featured-card--text::after { display: none; }
+.featured-card--text:hover { transform: translateY(-2px); }
+.featured-card--text .featured-card-copy { position: static; padding: 0; }
+.featured-card--text .featured-card-issue { color: var(--gold); }
+.featured-card--text .featured-card-series { color: var(--text); }
+.featured-card--text .featured-card-by { color: var(--text-2); }
+.featured-card--text .featured-card-by strong { color: var(--text); }
+.featured-card--text .featured-card-excerpt { font-size: 0.72rem; font-weight: 300; line-height: 1.8; color: var(--text-2); display: -webkit-box; -webkit-line-clamp: 4; -webkit-box-orient: vertical; overflow: hidden; margin-top: 8px; }
+.featured-card--text .featured-card-read { opacity: 1; transform: none; color: var(--gold); }
 .featured-image-drop { margin-top:6px; border:1px dashed var(--border-2); border-radius:var(--radius-sm); padding:22px; text-align:center; cursor:pointer; font-size:0.68rem; color:var(--text-2); transition:border-color 0.2s, color 0.2s; }
 .featured-image-drop:hover { border-color:rgba(200,160,106,0.5); color:var(--gold); }
 .featured-image-thumbs { display:flex; flex-wrap:wrap; gap:8px; margin-top:10px; }
@@ -694,22 +742,50 @@ nav {
 .featured-editor-thumb { width:44px; height:44px; border-radius:6px; object-fit:cover; border:1px solid var(--border); flex-shrink:0; }
 
 /* featured detail */
+.featured-detail-hero { position: relative; width: 100%; height: 82vh; min-height: 480px; max-height: 780px; overflow: hidden; margin-bottom: 44px; }
+.featured-detail-hero img { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; }
+.featured-detail-hero::after { content: ''; position: absolute; inset: 0; background: linear-gradient(to top, rgba(0,0,0,0.95) 0%, rgba(0,0,0,0.45) 40%, rgba(0,0,0,0.1) 68%, transparent 100%); }
+.featured-detail-hero-copy { position: absolute; left: 0; right: 0; bottom: 0; padding: 0 20px 52px; max-width: 820px; margin: 0 auto; z-index: 1; }
+.featured-detail-back-float { position: absolute; top: 100px; left: 20px; z-index: 2; }
 .featured-detail-wrap { padding: 0 20px 80px; max-width: 820px; margin: 0 auto; display: flex; flex-direction: column; gap: 28px; }
-.featured-detail-back { font-size: 0.62rem; font-weight: 500; letter-spacing: 0.1em; text-transform: uppercase; color: var(--text-3); cursor: pointer; display: inline-flex; align-items: center; gap: 8px; transition: color 0.2s; background: none; border: none; padding: 0; }
+.featured-detail-back { font-size: 0.62rem; font-weight: 500; letter-spacing: 0.1em; text-transform: uppercase; color: var(--text-3); cursor: pointer; display: inline-flex; align-items: center; gap: 8px; transition: color 0.2s; background: rgba(0,0,0,0.4); backdrop-filter: blur(6px); padding: 8px 14px; border-radius: var(--radius-full); border: 1px solid rgba(255,255,255,0.12); }
 .featured-detail-back::before { content: '←'; font-size: 0.8rem; letter-spacing: 0; }
 .featured-detail-back:hover { color: var(--gold); }
 .featured-detail-header { border-bottom: 1px solid var(--border); padding-bottom: 34px; }
 .featured-detail-issue { font-size: 0.6rem; font-weight: 500; letter-spacing: 0.12em; text-transform: uppercase; color: var(--gold); margin-bottom: 12px; display: flex; align-items: center; gap: 8px; }
 .featured-detail-issue::before { content: ''; width: 14px; height: 1px; background: var(--gold); }
+.featured-detail-hero-copy .featured-detail-issue { color: var(--gold-2); }
 .featured-detail-title { font-family: 'Cormorant Garamond', serif; font-size: clamp(2rem, 4vw, 3.2rem); font-weight: 300; font-style: italic; line-height: 1.1; letter-spacing: 0.02em; }
+.featured-detail-hero-copy .featured-detail-title { font-size: clamp(2.6rem, 6vw, 5rem); color: #fdfcf9; }
 .featured-detail-byline { margin-top: 16px; font-size: 0.7rem; font-weight: 300; color: var(--text-2); line-height: 1.9; }
 .featured-detail-byline strong { color: var(--text); font-weight: 400; }
+.featured-detail-hero-copy .featured-detail-byline { color: rgba(255,255,255,0.75); }
+.featured-detail-hero-copy .featured-detail-byline strong { color: #fff; }
 .featured-detail-story { font-family: 'Cormorant Garamond', serif; font-size: 1.05rem; font-weight: 300; line-height: 2.05; color: var(--text); white-space: pre-line; }
 .featured-detail-credits-box { background: var(--bg-1); border-radius: var(--radius-sm); border: 1px solid var(--border); padding: 26px 30px; }
 .featured-detail-credits-h { font-size: 0.6rem; font-weight: 500; letter-spacing: 0.12em; text-transform: uppercase; color: var(--gold); margin-bottom: 12px; display: flex; align-items: center; gap: 8px; }
 .featured-detail-credits-h::before { content: ''; width: 14px; height: 1px; background: var(--gold); }
 .featured-detail-credits-txt { font-size: 0.74rem; font-weight: 300; line-height: 1.9; color: var(--text-2); white-space: pre-line; }
 
+/* the spread — gallery */
+.featured-spread-label { font-size: 0.6rem; font-weight: 500; letter-spacing: 0.12em; text-transform: uppercase; color: var(--gold); display: flex; align-items: center; gap: 8px; }
+.featured-spread-label::before { content: ''; width: 14px; height: 1px; background: var(--gold); }
+.featured-spread-gallery { column-count: 2; column-gap: 12px; }
+.featured-spread-gallery img { width: 100%; display: block; border-radius: var(--radius-sm); margin-bottom: 12px; cursor: zoom-in; break-inside: avoid; border: 1px solid var(--border); transition: opacity 0.2s; }
+.featured-spread-gallery img:hover { opacity: 0.88; }
+@media (max-width: 640px) { .featured-spread-gallery { column-count: 1; } }
+
+/* lightbox */
+.fs-lightbox { position: fixed; inset: 0; background: rgba(0,0,0,0.94); z-index: 9999; display: none; align-items: center; justify-content: center; padding: 40px; }
+.fs-lightbox.open { display: flex; }
+.fs-lightbox img { max-width: 100%; max-height: 90vh; border-radius: 4px; object-fit: contain; }
+.fs-lightbox-close, .fs-lightbox-nav { position: absolute; background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.16); color: #fff; width: 42px; height: 42px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 1.1rem; cursor: pointer; transition: background 0.2s; }
+.fs-lightbox-close:hover, .fs-lightbox-nav:hover { background: rgba(255,255,255,0.18); }
+.fs-lightbox-close { top: 24px; right: 24px; }
+.fs-lightbox-nav.prev { left: 24px; top: 50%; transform: translateY(-50%); }
+.fs-lightbox-nav.next { right: 24px; top: 50%; transform: translateY(-50%); }
+@media (max-width: 640px) { .featured-detail-back-float { top: 84px; } .featured-detail-hero { height: 58vh; } }
+
 /* ═══════════ ADMIN ═══════════ */
 .admin-wrap { padding: 22px 20px; max-width: 1160px; margin: 0 auto; display: flex; flex-direction: column; gap: 12px; }
 .admin-gate-wrap { display: flex; justify-content: center; }
@@ -1465,10 +1541,15 @@ footer {
   FEATURED DETAIL
 ══════════════════════════════════ -->
 <div id="featured-detail" class="pg">
-  <div class="featured-detail-wrap">
-    <button class="featured-detail-back" onclick="go('featured')">Back to Featured</button>
-    <div id="featured-detail-content"></div>
-  </div>
+  <div id="featured-detail-content"></div>
+</div>
+
+<!-- ─── FEATURED GALLERY LIGHTBOX ─── -->
+<div class="fs-lightbox" id="fs-lightbox" onclick="if(event.target===this)closeLightbox()">
+  <button class="fs-lightbox-close" onclick="closeLightbox()">✕</button>
+  <button class="fs-lightbox-nav prev" onclick="lightboxStep(-1)">←</button>
+  <img id="fs-lightbox-img" src="" alt="">
+  <button class="fs-lightbox-nav next" onclick="lightboxStep(1)">→</button>
 </div>
 
 <!-- ══════════════════════════════════
@@ -2280,21 +2361,63 @@ async function promoteToFeatured(subId, name) {
   } catch (err) { console.error('[promoteToFeatured]', err); }
 }
 
+function featuredCardHtml(f, wide) {
+  const urls = f.imageUrls || [];
+  if (urls.length === 0) {
+    return '<div class="featured-card featured-card--text" data-fid="' + escHtml(f.id) + '" onclick="viewFeatured(this.dataset.fid)">' +
+      '<div class="featured-card-copy">' +
+      '<div class="featured-card-issue">' + escHtml(f.issue) + '</div>' +
+      '<div class="featured-card-series">' + escHtml(f.series) + '</div>' +
+      '<div class="featured-card-by">By <strong>' + escHtml(f.photographer) + '</strong></div>' +
+      '<div class="featured-card-excerpt">' + escHtml((f.story || '').substring(0,220)) + '</div>' +
+      '<div class="featured-card-read">Read the spread</div>' +
+      '</div></div>';
+  }
+  return '<div class="featured-card' + (wide ? ' is-wide' : '') + '" data-fid="' + escHtml(f.id) + '" onclick="viewFeatured(this.dataset.fid)">' +
+    '<img src="' + escHtml(urls[0]) + '" alt="' + escHtml(f.series) + '" loading="lazy">' +
+    '<div class="featured-card-copy">' +
+    '<div class="featured-card-issue">' + escHtml(f.issue) + '</div>' +
+    '<div class="featured-card-series">' + escHtml(f.series) + '</div>' +
+    '<div class="featured-card-by">By <strong>' + escHtml(f.photographer) + '</strong></div>' +
+    '<div class="featured-card-read">View the spread</div>' +
+    '</div></div>';
+}
+
 async function loadFeatured() {
   if (_featuredLoaded) return;
   const container = document.getElementById('featured-grid-container'); if (!container) return;
   try {
     const res = await fetch(`${API}/api/featured`);
     const data = await res.json(); _featuredLoaded = true;
-    if (!data.featured || data.featured.length === 0) { container.innerHTML = '<div class="featured-empty">The first issue has not yet been featured. Check back after Issue 01 launches.</div>'; return; }
-    container.innerHTML = '<div class="featured-grid">' + data.featured.map(function(f) { return '<div class="featured-card" data-fid="' + escHtml(f.id) + '" onclick="viewFeatured(this.dataset.fid)"><div class="featured-card-issue">' + escHtml(f.issue) + '</div><div class="featured-card-series">' + escHtml(f.series) + '</div><div class="featured-card-by">By <strong>' + escHtml(f.photographer) + '</strong></div>' + (f.instagram ? '<div class="featured-card-insta">' + escHtml(f.instagram) + '</div>' : '') + '<div class="featured-card-excerpt">' + escHtml((f.story || '').substring(0,400)) + '</div><div class="featured-card-read">Read the spread</div></div>'; }).join('') + '</div>';
+    const items = data.featured || [];
+    if (items.length === 0) { container.innerHTML = '<div class="featured-empty">The first issue has not yet been featured. Check back after Issue 01 launches.</div>'; return; }
+
+    const heroIndex = items.findIndex(function(f) { return (f.imageUrls || []).length > 0; });
+    const hero = heroIndex >= 0 ? items[heroIndex] : null;
+    const rest = hero ? items.filter(function(_, i) { return i !== heroIndex; }) : items;
+
+    let html = '';
+    if (hero) {
+      html += '<div class="featured-hero" data-fid="' + escHtml(hero.id) + '" onclick="viewFeatured(this.dataset.fid)">' +
+        '<img src="' + escHtml(hero.imageUrls[0]) + '" alt="' + escHtml(hero.series) + '">' +
+        '<div class="featured-hero-copy">' +
+        '<div class="featured-hero-kicker">' + escHtml(hero.issue) + ' &nbsp;·&nbsp; Cover story</div>' +
+        '<div class="featured-hero-title">' + escHtml(hero.series) + '</div>' +
+        '<div class="featured-hero-by">By <strong>' + escHtml(hero.photographer) + '</strong>' + (hero.instagram ? ' &nbsp;·&nbsp; <span>' + escHtml(hero.instagram) + '</span>' : '') + '</div>' +
+        '</div></div>';
+    }
+    if (rest.length) {
+      html += '<div class="featured-grid">' + rest.map(function(f, i) { return featuredCardHtml(f, i % 5 === 0); }).join('') + '</div>';
+    }
+    container.innerHTML = html;
   } catch { container.innerHTML = '<div class="featured-empty">Could not load featured content.</div>'; _featuredLoaded = false; }
 }
 
 async function viewFeatured(id) {
   const contentEl = document.getElementById('featured-detail-content'); if (!contentEl) return;
-  contentEl.innerHTML = '<div style="padding:40px;font-size:0.74rem;color:var(--text-3)">Loading…</div>';
+  contentEl.innerHTML = '<div style="padding:80px 40px;font-size:0.74rem;color:var(--text-3);text-align:center">Loading…</div>';
   go('featured-detail');
+  window.scrollTo(0, 0);
   try {
     if (_featuredCache[id]) { renderFeaturedDetail(_featuredCache[id]); return; }
     const res = await fetch(`${API}/api/featured/${encodeURIComponent(id)}`);
@@ -2304,8 +2427,61 @@ async function viewFeatured(id) {
 
 function renderFeaturedDetail(f) {
   const el = document.getElementById('featured-detail-content'); if (!el || !f) return;
-  el.innerHTML = '<div class="featured-detail-header"><div class="featured-detail-issue">' + escHtml(f.issue) + '</div><div class="featured-detail-title">' + escHtml(f.series) + '</div><div class="featured-detail-byline">By <strong>' + escHtml(f.photographer) + '</strong>' + (f.instagram ? ' &nbsp;·&nbsp; <span style="color:var(--gold)">' + escHtml(f.instagram) + '</span>' : '') + '</div></div><div class="featured-detail-story">' + escHtml(f.story) + '</div><div class="featured-detail-credits-box"><div class="featured-detail-credits-h">Team Credits</div><div class="featured-detail-credits-txt">' + escHtml(f.credits) + '</div></div>';
-}
+  const urls = f.imageUrls || [];
+  const heroUrl = urls[0];
+  const galleryUrls = urls.slice(1);
+  _currentGallery = urls;
+
+  const backBtn = '<button class="featured-detail-back' + (heroUrl ? ' featured-detail-back-float' : '') + '" onclick="go(\'featured\')">Back to Featured</button>';
+
+  let html = '';
+  if (heroUrl) {
+    html += backBtn + '<div class="featured-detail-hero">' +
+      '<img src="' + escHtml(heroUrl) + '" alt="' + escHtml(f.series) + '">' +
+      '<div class="featured-detail-hero-copy">' +
+      '<div class="featured-detail-issue">' + escHtml(f.issue) + '</div>' +
+      '<div class="featured-detail-title">' + escHtml(f.series) + '</div>' +
+      '<div class="featured-detail-byline">By <strong>' + escHtml(f.photographer) + '</strong>' + (f.instagram ? ' &nbsp;·&nbsp; <span style="color:var(--gold-2)">' + escHtml(f.instagram) + '</span>' : '') + '</div>' +
+      '</div></div>';
+  } else {
+    html += '<div class="featured-detail-wrap" style="padding-top:100px;padding-bottom:0">' + backBtn + '</div>';
+  }
+
+  html += '<div class="featured-detail-wrap">';
+  if (!heroUrl) {
+    html += '<div class="featured-detail-header"><div class="featured-detail-issue">' + escHtml(f.issue) + '</div><div class="featured-detail-title">' + escHtml(f.series) + '</div><div class="featured-detail-byline">By <strong>' + escHtml(f.photographer) + '</strong>' + (f.instagram ? ' &nbsp;·&nbsp; <span style="color:var(--gold)">' + escHtml(f.instagram) + '</span>' : '') + '</div></div>';
+  }
+  html += '<div class="featured-detail-story">' + escHtml(f.story) + '</div>';
+  html += '<div class="featured-detail-credits-box"><div class="featured-detail-credits-h">Team Credits</div><div class="featured-detail-credits-txt">' + escHtml(f.credits) + '</div></div>';
+  if (galleryUrls.length) {
+    html += '<div class="featured-spread-label">The spread</div>';
+    html += '<div class="featured-spread-gallery">' + galleryUrls.map(function(u, i) { return '<img src="' + escHtml(u) + '" alt="" loading="lazy" onclick="openLightbox(' + (i + 1) + ')">'; }).join('') + '</div>';
+  }
+  html += '</div>';
+
+  el.innerHTML = html;
+}
+
+let _currentGallery = [];
+let _lightboxIndex = 0;
+function openLightbox(index) {
+  if (!_currentGallery.length) return;
+  _lightboxIndex = index;
+  document.getElementById('fs-lightbox-img').src = _currentGallery[_lightboxIndex];
+  document.getElementById('fs-lightbox').classList.add('open');
+}
+function closeLightbox() { document.getElementById('fs-lightbox').classList.remove('open'); }
+function lightboxStep(dir) {
+  if (!_currentGallery.length) return;
+  _lightboxIndex = (_lightboxIndex + dir + _currentGallery.length) % _currentGallery.length;
+  document.getElementById('fs-lightbox-img').src = _currentGallery[_lightboxIndex];
+}
+document.addEventListener('keydown', function(e) {
+  if (!document.getElementById('fs-lightbox').classList.contains('open')) return;
+  if (e.key === 'Escape') closeLightbox();
+  if (e.key === 'ArrowLeft') lightboxStep(-1);
+  if (e.key === 'ArrowRight') lightboxStep(1);
+});
 
 function escHtml(str) {
   if (typeof str !== 'string') return '';
